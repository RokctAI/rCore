# Plan: Consolidate call_groq.py into The-Rokct-Protocol

## Goal
Move `call_groq.py` from factory to The-Rokct-Protocol and turn factory's copy into a wrapper, creating a unified agent delegation pattern.

## Current State
- `factory/.rokct/skills/agent_delegation/scripts/call_jules.py` → wraps `delegate_to_agent.py` (The-Rokct-Protocol)
- `factory/.rokct/skills/agent_delegation/scripts/call_groq.py` → standalone, NOT in The-Rokct-Protocol
- `The-Rokct-Protocol/core/skills/agent_delegation/scripts/delegate_to_agent.py` → canonical Jules agent CLI

## Proposed Changes

### Option A: Add Groq to delegate_to_agent.py (Recommended)
Merge Groq functionality into `delegate_to_agent.py` as an additional agent type.

**Benefits:**
- Single unified CLI for all agents
- Consistent interface (`--agent-type jules|groq`)
- Factory wrapper `call_jules.py` continues working unchanged

**Changes:**
1. Add `call_groq()` method to `AgentCLI` class in `delegate_to_agent.py`
2. Add `--agent-type` argument (default: jules) via CLI
3. Add `call_groq` command to `AgentCLI` class
4. Convert factory's `call_groq.py` to wrapper pattern (like `call_jules.py`)

### Option B: Separate file in The-Rokct-Protocol
Keep `call_groq.py` separate but in The-Rokct-Protocol, with factory wrapper.

**Changes:**
1. Move `call_groq.py` to `The-Rokct-Protocol/core/skills/agent_delegation/scripts/`
2. Update factory's `call_groq.py` to wrapper pattern

## Recommended: Option A

### Files to Create/Modify:
1. `The-Rokct-Protocol/core/skills/agent_delegation/scripts/delegate_to_agent.py`
   - Add `call_groq()` static method
   - Add `--agent-type` CLI argument
   - Add Groq command support to `AgentCLI`

2. `factory/.rokct/skills/agent_delegation/scripts/call_groq.py`
   - Convert to wrapper pattern (copy from `call_jules.py`)

## Implementation Steps
1. Add Groq methods to `delegate_to_agent.py`
2. Update factory's `call_groq.py` wrapper
3. Test both Jules and Groq invocation